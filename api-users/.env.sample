# API Users Service Configuration

# Server Port
PORT=5001

# PostgreSQL Database
# Docker: postgresql://filmly:filmly@postgres:5432/filmly
# Local: postgresql://filmly:filmly@localhost:5433/filmly
DATABASE_URL=postgresql://filmly:filmly@postgres:5432/filmly

# JWT Configuration
# MUST match api-catalog JWT_SECRET
# Production: Use a strong random string (32+ characters)
# Generate: openssl rand -hex 32
JWT_SECRET=dev-secret-change-me
JWT_ALG=HS256

# Token Expiration (in minutes)
# Default: 10080 minutes = 7 days
JWT_EXPIRES_MIN=10080
ACCESS_TOKEN_EXPIRE_MINUTES=10080
